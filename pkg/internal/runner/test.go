package runner

func Test() int {
	// r.runLock.Lock()
	// defer r.runLock.Unlock()
	//
	// cmd := exec.CommandContext(ctx, "go", "test", r.DevcardMeta.ImportPath)
	// cmd.Dir = r.Dir
	// b, err := cmd.CombinedOutput()
	// if err == nil {
	// 	return 0
	// }
	//
	// failedTests := 0
	// for _, line := range strings.Split(string(b), "\n") {
	// 	if strings.HasPrefix(line, "--- FAIL:") {
	// 		failedTests++
	// 	}
	// }
	//
	// return failedTests

	return 0
}
